export const baseItemState = (state) => state.base.data.item;
export const allDragonsState = (state) => state.base.data.allDragons;
export const loadingState = (state) => state.base.data.loading;
export const errorSelector = (state) => state.base.data.error;
